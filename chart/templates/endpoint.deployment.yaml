apiVersion: apps/v1
kind: Deployment
metadata:
  name: endpoint
  labels:
    app: krateo.io
    component: endpoint
spec:
  replicas: 1
  selector:
    matchLabels:
      component: endpoint
  template:
    metadata:
      labels:
        component: endpoint
    spec:
      containers:
        - name: endpoint
          image: {{ .Values.registry.images.endpoint }}
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          livenessProbe:
            httpGet:
              path: /ping
              port: 8080
            timeoutSeconds: 5
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /ping
              port: 8080
            timeoutSeconds: 5
            periodSeconds: 30
          env:
            - name: 'LOG_LEVEL'
              value: {{ .Values.log.level }}
            - name: 'MONGODB_URI'
              valueFrom:
                secretKeyRef:
                  name: krateo-secret
                  key: MONGODB_URI
      imagePullSecrets:
        - name: registry-credentials
